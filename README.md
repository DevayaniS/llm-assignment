# LLM Assignment

This repository contains the notebook `LLM-Assignment.ipynb`, intended to be run on Google Colab and to use the OpenAI API.

## Run on Google Colab
1. Open `LLM-Assignment.ipynb` in Google Colab (upload it, or open it from GitHub).
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
